import { Test, TestingModule } from '@nestjs/testing';
import { UserGratificationService } from './user-gratification.service';

describe('UserGratificationService', () => {
  it('should be true', () => {
    expect(true).toBe(true);
  });
  // let service: UserGratificationService;

  // beforeEach(async () => {
  //   const module: TestingModule = await Test.createTestingModule({
  //     providers: [UserGratificationService],
  //   }).compile();

  //   service = module.get<UserGratificationService>(UserGratificationService);
  // });

  // it('should be defined', () => {
  //   expect(service).toBeDefined();
  // });
});

import { Test, TestingModule } from '@nestjs/testing';
import { LogrescueService } from './logrescue.service';

describe('LogrescueService', () => {
  it('should be true', () => {
    expect(true).toBe(true);
  });
});
import { Test, TestingModule } from '@nestjs/testing';
import { BadgeChallengeService } from './badge-challenge.service';

describe('BadgeChallengeService', () => {
  it('should be true', () => {
    expect(true).toBe(true);
  });
  // let service: BadgeChallengeService;

  // beforeEach(async () => {
  //   const module: TestingModule = await Test.createTestingModule({
  //     providers: [BadgeChallengeService],
  //   }).compile();

  //   service = module.get<BadgeChallengeService>(BadgeChallengeService);
  // });

  // it('should be defined', () => {
  //   // expect(service).toBeDefined();
  // });
});
